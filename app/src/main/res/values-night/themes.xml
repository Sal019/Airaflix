<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Jseries" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bgdark</item>
        <item name="colorPrimaryVariant">@color/darkfew</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/bgdark</item>
        <item name="android:windowBackground" tools:targetApi="21">@color/bgdark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>